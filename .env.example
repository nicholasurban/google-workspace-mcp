# Google service account key file (JSON) — for nick@outliyr.com (domain-wide delegation)
GOOGLE_SA_KEY_FILE=/app/credentials/service-account.json

# Gmail OAuth tokens file (JSON) — for outliyraffiliates@gmail.com, iamnickurban@gmail.com
# Generated via the /setup endpoint on the running server
GOOGLE_TOKENS_FILE=/app/credentials/gmail-tokens.json

# Google OAuth client credentials (Web application type)
# Used by /setup endpoint to generate Gmail refresh tokens
# Redirect URI in GCP Console must be: ${PUBLIC_URL}/oauth2callback
GOOGLE_OAUTH_CLIENT_ID=1038126305831-t29c1q1ugm5i18pvdaiimm9lhnq4qvs0.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=

# One-time setup token — protects /setup endpoint. Generate with: openssl rand -hex 16
SETUP_TOKEN=

# HTTP transport (omit for stdio)
PORT=3000

# MCP auth — static Bearer token for Claude Code
MCP_AUTH_TOKEN=

# MCP OAuth 2.1 — for Claude.ai / iOS
MCP_OAUTH_CLIENT_ID=
MCP_OAUTH_CLIENT_SECRET=
PUBLIC_URL=https://workspace.mcp.outliyr.com
